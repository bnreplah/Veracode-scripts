[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    unit: Pure unit tests with no external dependencies
    integration: Integration tests invoking scripts end-to-end
    api: Tests requiring live Veracode API credentials
    slow: Tests that may take significant time to run
